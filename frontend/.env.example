# Frontend build/runtime config
# Base path the SPA calls; defaults to /api/v1 (proxied to the backend in dev).
VITE_API_BASE=/api/v1
# Dev-server proxy target for /api requests.
VITE_API_TARGET=http://localhost:8000
